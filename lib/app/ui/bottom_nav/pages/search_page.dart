import 'package:flutter/material.dart';

class SearchPage22 extends StatelessWidget {
  const SearchPage22({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SearchPage'),
    );
  }
}
